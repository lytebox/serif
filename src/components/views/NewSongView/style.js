import styled, { css } from "styled-components";
import theme from "../../../theme";

const NewSongViewWrapper = styled.div`
  display: flex;
  flex: 1 0 auto;
  flex-direction: column;
`;

export { NewSongViewWrapper };
